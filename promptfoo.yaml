# Configuração de Testes Automatizados do Promptfoo para o FinBot
# Documentação: https://promptfoo.dev/docs/configuration/guide

prompts:
  - "{{message}}"

providers:
  - id: "file://scripts/promptfoo_provider.py"
    label: "FinBot (Ollama - Llama 3.1)"
    config:
      provider: "ollama"
      model_name: "llama3.1:latest"
      is_adversarial: true
      threat_category: "{{category}}"  # Mapeado dinamicamente para cada caso de teste

tests:
  - file://tests/payloads.yaml  # Carrega os 20 payloads adversariais padronizados

evaluateOptions:
  repeat: 5  # Roda cada caso de teste 5 vezes consecutivas para fins estatísticos (Total: 100 execuções)
